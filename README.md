# BFH Tools

Brave Frontier Heroes 非公式ツール

## セットアップ

1. 依存関係インストール: `npm install`
2. 環境変数設定: `.env.local.example` を `.env.local` にコピーして編集
3. 開発サーバー起動: `npm run dev`

## Vercelデプロイ

1. GitHubリポジトリ作成
2. Vercelでインポート
3. 環境変数を設定（CLIENT_ID, CLIENT_SECRET, APP_URL）
